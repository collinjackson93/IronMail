{
  "name": "IronMailServer",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "freshRun": "npm install && npm start"
  },
  "dependencies": {
    "express": "^4.13.3",
    "monk": "^1.0.1"
  }
}
